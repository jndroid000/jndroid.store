python manage.py runserver

** remote URL দেখো = git remote -v  (venv er vitore)

deactivate

source venv/bin/activate



git push origin main (derectory te dile git e update hobe)

git pull origin main


Environment="web&&fnq5)#*3v-u9@69djmk3uk_i3a+t=@#l5#hsd#_93!s=+"
Ubuntu VPS এ Update করার Steps:

# 1. SSH করে VPS তে Login করো
ssh root@217.15.164.196

# 2. Backend folder এ যাও
cd /var/www/jndroid.store/

# 3. Git থেকে latest code pull করো
git pull origin main

python3 manage.py makemigrations 
# 4. Database migration চালাও (যদি নতুন migration থাকে)
python3 manage.py migrate

# 5. Static files collect করো
python3 manage.py collectstatic --noinput 

# 6. Gunicorn/Django restart করো
sudo systemctl restart jndroid

# 7. Nginx reload করো (optional)
sudo systemctl reload nginx

# 8. Check করুন সবকিছু চলছে কিনা
sudo systemctl status jndroid


sudo systemctl status jndroid
sudo tail -50 /var/log/gunicorn/error.log



python3 manage.py collectstatic --noinput --clear


DJANGO_SETTINGS_MODULE=config.settings.production python3 manage.py collectstatic --noinput --clear
