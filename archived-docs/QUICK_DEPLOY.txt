╔════════════════════════════════════════════════════════════════════════════════╗
║              QUICK DEPLOYMENT REFERENCE - JN APP STORE                          ║
║                    PostgreSQL Production Setup                                   ║
╚════════════════════════════════════════════════════════════════════════════════╝

🔐 DATABASE CREDENTIALS (Keep Secret!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Database:    jndroid_db
User:        postgres
Password:    522475
Host:        localhost
Port:        5432


⚡ QUICK COMMANDS (Copy & Paste)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  INSTALL PSYCOPG2
   pip install psycopg2-binary==2.9.9

2️⃣  TEST POSTGRESQL CONNECTION
   python verify_postgres.py

3️⃣  SET PRODUCTION ENVIRONMENT (Windows)
   set DJANGO_ENV=production

4️⃣  GENERATE NEW SECRET KEY
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

5️⃣  VERIFY PRODUCTION SETTINGS
   python manage.py check --deploy

6️⃣  RUN DATABASE MIGRATIONS
   python manage.py migrate

7️⃣  CREATE ADMIN USER
   python manage.py createsuperuser

8️⃣  COLLECT STATIC FILES
   python manage.py collectstatic --noinput

9️⃣  SEND TEST EMAIL
   python manage.py shell
   >>> from django.core.mail import send_mail
   >>> send_mail('Test', 'Test message', 'jndroid000@gmail.com', ['recipient@example.com'])

🔟 RUN PRODUCTION SERVER (DEV)
   python manage.py runserver

☑️  PRODUCTION SERVER (LIVE - Use Gunicorn)
    gunicorn config.wsgi:application --bind 0.0.0.0:8000


📋 STEP-BY-STEP CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before Starting:
   ☐ Backup existing SQLite database (db.sqlite3)
   ☐ Have PostgreSQL installed
   ☐ Know database password: 522475

Setup Phase:
   ☐ pip install psycopg2-binary==2.9.9
   ☐ Create PostgreSQL database: jndroid_db
   ☐ Update .env.production with SECRET_KEY
   ☐ python verify_postgres.py (should pass all tests)
   ☐ set DJANGO_ENV=production

Initialization:
   ☐ python manage.py migrate
   ☐ python manage.py createsuperuser
   ☐ python manage.py collectstatic --noinput
   ☐ python manage.py check --deploy

Testing:
   ☐ python manage.py runserver
   ☐ Open http://localhost:8000
   ☐ Test login page
   ☐ Test app upload
   ☐ Test admin panel
   ☐ Check /logs/ directory for errors

Going Live:
   ☐ Update DJANGO_ALLOWED_HOSTS for domain
   ☐ Set SSL certificate
   ☐ Configure domain DNS
   ☐ Use Gunicorn + Nginx
   ☐ Monitor logs


🐘 POSTGRESQL SETUP (one-time)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open PostgreSQL Command Line (psql) and run:

CREATE DATABASE jndroid_db;
GRANT ALL PRIVILEGES ON DATABASE jndroid_db TO postgres;
ALTER DATABASE jndroid_db OWNER TO postgres;
ALTER USER postgres WITH PASSWORD '522475';


🔒 SECURITY CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   ☐ Generate new SECRET_KEY
   ☐ Update EMAIL_HOST_PASSWORD
   ☐ Set DJANGO_ALLOWED_HOSTS correctly
   ☐ Enable DEBUG=False (already set)
   ☐ Configure SSL/HTTPS
   ☐ Set secure cookies (already configured)
   ☐ Enable HSTS headers (already configured)
   ☐ Configure CSRF_TRUSTED_ORIGINS


📂 FILE LOCATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Configuration Files:
   /backend/.env.production              ← Environment variables
   /config/settings/production.py        ← Django settings
   /config/settings/base.py              ← Base settings
   /config/settings/development.py       ← Dev settings

Verification Script:
   /backend/verify_postgres.py           ← Run to verify setup

Documentation:
   /backend/POSTGRESQL_SETUP.md          ← Full setup guide
   /backend/POSTGRESQL_CONFIG_SUMMARY.md ← This summary
   /backend/QUICK_DEPLOY.txt             ← Quick reference (this file)

Database:
   /backend/db.sqlite3                   ← Old SQLite (can be deleted after migration)
   jndroid_db                            ← PostgreSQL production DB

Logs:
   /backend/logs/django.log              ← Application logs
   /backend/logs/errors.log              ← Error logs
   /backend/logs/security.log            ← Security logs


🆘 QUICK HELP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ERROR: psycopg2 not found
   → pip install psycopg2-binary==2.9.9

ERROR: Cannot connect to PostgreSQL
   → Check if PostgreSQL service is running
   → Check credentials in .env.production
   → Verify database exists: CREATE DATABASE jndroid_db;

ERROR: Migration fails
   → set DJANGO_ENV=production
   → python manage.py migrate

ERROR: "password authentication failed"
   → Check password 522475 in .env.production and PostgreSQL

ERROR: DEBUG should be False
   → Automatically set in production.py
   → Just make sure: set DJANGO_ENV=production

ERROR: Static files not found
   → python manage.py collectstatic --noinput

ERROR: 500 Server Error
   → Check logs: /logs/errors.log
   → Run: python manage.py check --deploy


📞 IMPORTANT CONTACTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Admin Email:    jndroid000@gmail.com
Database Name:  jndroid_db
Database User:  postgres
Domain:         jndroid.store


✨ COMPLETED SETUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ PostgreSQL configured with your credentials (jndroid_db / 522475)
✓ Production settings enhanced and validated
✓ Verification script created (verify_postgres.py)
✓ Complete documentation provided
✓ Security headers configured
✓ Connection pooling enabled
✓ Atomic requests enabled
✓ SSL support configured

Next: Run verification script
   python verify_postgres.py
